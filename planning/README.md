# Planning

Planning-with-files (PWF) artifacts for structured task execution.

```
planning/
  active/           <- current work (task_plan.md, findings.md, progress.md)
  archive/          <- completed issues (YYYY-MM-issue-N-slug/)
```

See the feature-workflow convention in `CLAUDE.md`. Use `/planning-init <N>` to start an issue.
